-- =====================================================
-- FIX MISSING - Hjelpefil for å fikse manglende data
-- =====================================================
-- Organisasjoner opprettes via registrering på skyplanner.no
-- Ingen hardkodede demo-data - alt er dynamisk per bedrift

-- Vis eksisterende organisasjoner
SELECT id, navn, slug FROM organizations;
