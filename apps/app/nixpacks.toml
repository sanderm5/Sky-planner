[phases.setup]
cmds = ["npm install -g pnpm"]

[phases.install]
dependsOn = ["setup"]
cmds = ["cd ../.. && pnpm install --no-frozen-lockfile"]

[phases.build]
dependsOn = ["install"]
cmds = ["cd ../.. && pnpm --filter @skyplanner/app build"]

[start]
cmd = "HOST=0.0.0.0 npx tsx dist/server.js"
